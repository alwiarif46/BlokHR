'use client'

/**
 * @deprecated Secondary pages use PageFrame + PageHero + intent sections.
 * Kept only so accidental imports fail loudly at the type level if re-added.
 */
export function MarketingPage() {
  throw new Error('MarketingPage was retired. Use PageFrame + PageHero + intent sections.')
}
